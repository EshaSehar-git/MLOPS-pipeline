fastapi==0.115.0
uvicorn[standard]==0.30.6
pytesseract==0.3.13
easyocr==1.7.2
pillow==10.4.0
pydantic==2.9.2
mlflow==2.16.2
evidently==0.4.33
pandas==2.2.3
numpy==1.26.4
python-multipart==0.0.9
scikit-learn==1.5.2

# Needed to serve the fine-tuned LayoutLMv3 model at inference time (api/main.py
# falls back to the rule-based extractor if these aren't importable, but the
# real model never loads without them).
transformers==5.16.1
torch==2.14.0

# Only needed for the LayoutLMv3 training path (install separately in Colab or a training env)
# datasets==2.21.0
# seqeval==1.2.2
# accelerate==0.34.2
